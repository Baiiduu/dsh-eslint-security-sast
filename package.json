{
  "name": "@aaub-software/dsh-eslint-security-sast-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Development workspace for the DeepSeek Harness ESLint Security SAST bundle.",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "pnpm --recursive run build",
    "typecheck": "pnpm --recursive run typecheck",
    "test": "pnpm --recursive run test"
  }
}
